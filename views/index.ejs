<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ChatBoard</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <h1><strong>All Chats!</strong></h1>
    <hr />
    <form method="get" action="/chats/new">
      <button class="btn btn-outline-success">New Chat</button>
    </form>
    <br />
    <div class="chat-container">
      <% for(let chat of chats) { %>
      <div class="chat card mb-5 me-5">
        <h4 class="card-header">
          <strong>From: <i><%= chat.from %></i></strong>
        </h4>
        <div class="msg card-body">
          <p class="card-text"><%= chat.message %></p>
          <div class="buttons">
            <form method="get" action="/chats/<%= chat._id %>/edit">
              <button class="btn">edit</button>
            </form>
            <form method="post" action="/chats/<%= chat._id %>?_method=DELETE">
              <button class="btn">delete</button>
            </form>
          </div>
        </div>
        <h4 class="card-footer">
          <em>Received by <i><%= chat.to %></i></em>
        </h4>
        <div class="time">
          <p style="margin: 0px 15px">
            <%= chat.created_at.toString().split(" ")[4] %>
          </p>
          <p>
            <%= chat.created_at.toString().split(" ").slice(0,4).join(" ") %>
          </p>
        </div>
      </div>
      <br /><br />
      <% } %>
    </div>
  </body>
</html>
